// FA Engine stub — game uses monolithic game.js architecture
// This file satisfies the strategy-rpg template structure.
(function(window) {
  'use strict';
  if (!window.FA) window.FA = {};
  FA.getState = FA.getState || function() { return {}; };
  FA.setState = FA.setState || function(k, v) {};
  FA.on = FA.on || function() {};
  FA.emit = FA.emit || function() {};
  FA.register = FA.register || function() {};
  FA.lookup = FA.lookup || function() { return null; };
  FA.addLayer = FA.addLayer || function() {};
})(window);
